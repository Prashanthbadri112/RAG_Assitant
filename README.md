This a  Retrieval-Augmented Generation (RAG) Application. 
It allows users to upload their own pdf documents (e.g., textbooks), and the system intelligently 
answers queries by retrieving the most relevant content before generating a final response.
